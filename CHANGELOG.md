# Changelog

版本号按 [semver](https://semver.org/lang/zh-CN/)。**行为变了就是 minor**，
哪怕改动很小 —— 一个悄悄换了含义的异常比一个新函数危险得多。

---

## 0.16.0

- `slot()` / `pick()` 改为原生单成员查询，增加有界快照和字段摘要缓存，批量结果
  使用快速解码，仍保持 state 副本、精确筛选和版本一致性。
- `join(coalesce_ms=50)` 显式控制变化合并间隔，默认行为不变，间隔受续租预算限制。
- 增加 `Call`、`batch()`、`abatch()` 和 `BatchError`；批次顺序执行、遇错停止，
  保留逐项 fencing 和可核对的请求 ID，不自动重试，也不承诺事务。
- RPC 的普通 JSON 使用兼容的快速路径；不支持的值、编码和错误仍遵循原有语义。
- 注册中心跳过已同步游标的复制，只扫描所需历史后缀，有界共享 delta，并在锁外
  通知等待者和释放旧缓存。
- 基准失败或缺失指标不再悄悄通过；修复资源清理、指标命名和拓扑，记录构建与环境
  来源，并加入单成员查询、独立服务进程、批量调用和变化间隔场景。

---

## 0.15.0

- 修复 fencing 后仍能使用旧 epoch、同一事件循环重入后 watcher 丢失唤醒，以及
  持续变化绕过超时的问题。
- 发布拒绝时保持原状态；`NaN` 和无穷值当场报错。`join()` 失败会释放已创建的
  心跳和方法服务器。`CallContext` 注入保留位置专用、可变参数和重复参数检查。
- 协议 2 引入任期内发布序号，防止延迟或取消请求覆盖已确认状态；兼容旧客户端，
  遇到不支持排序的旧注册中心时告警。修复短租约续租预算和大整数混合浮点筛选。
- 为上述场景增加回归测试；PR 和 main 推送自动运行质量检查，发布仅限 tag 或手动触发。
- Python 测试按子系统重组，回归用例归入对应功能，共享进程和网络辅助移入
  `tests/support/`；保留全部用例和 marker，并更新 mutation 选择器。

---

## 0.14.0

新增一个调用修饰符：**`.returns(T)` 把 JSON 结果恢复成调用端声明的 Python
类型。**

```python
job = h.pull_job.returns(AgentJob)()
jobs = await ah.pull_jobs.returns(list[AgentJob])()
```

JSON 留得住值，留不住类型。四层嵌套的 `NamedTuple` 过线后只剩四层 list，以前每个
client 都要重复写一遍拆包。现在调用端只声明目标类型，TinyRay 递归恢复
`NamedTuple`、dataclass、`TypedDict`、Enum、Optional/Union、Literal、tuple、
set、list/dict、datetime 和 UUID；`TypedDict` 仍是 dict，其中声明的
`NamedTuple` 字段会恢复成真正的 `NamedTuple`。

`.returns()` 和 `.timeout()` 都是单次调用修饰符，顺序任意：

```python
h.pull_job.timeout(5).returns(AgentJob)()
h.pull_job.returns(AgentJob).timeout(5)()
```

协议仍是普通 JSON，不在线上传 Python 类名，也不替服务端序列化任意对象。转换失败
抛本地 `TypeError`，消息带远端身份、方法名、目标类型和出错的 JSON 路径；远端方法
此时已经成功运行，失败只发生在结果恢复阶段。

实现复用服务端参数转换已经依赖的 `msgspec`，没有引入新依赖。七个端到端场景覆盖
同步、异步和各种嵌套结构；六条新 mutant 全部被测试抓住。完整 Python 套件
446 passed。

---

## 0.13.3

内容就是 0.13.2 —— **0.13.2 打了 tag 但没有发出去**，CI 的 example 关卡卡在
`14_gil_heartbeat` 上，PyPI 上没有那个版本号。真正的改动是下面 0.13.2 那条
backlog 修复；从 0.13.1 升上来的人在这一版拿到它。

顺带治了卡住发布的那个 example。它不进 wheel，只影响 CI，但它已经实打实卡了两次
发布，所以值得记一笔：这个例子让**被饿死的主线程去释放自己的 hog** —— 睡满
hold，醒来设一个 flag 给 hog 轮询。可整个例子要证明的就是"跑 Python 的什么都轮不
上"，而那一次 `big * big` 在空闲核上就要 1666.8ms，全程握着 GIL。两核实测，
`stop.set()` 落在 hold 结束之后的 6 到 14 秒。

join 让它更糟：十七个线程各给 5 秒，就是 85 秒，本身就比 driver 留给整个例子的
90 秒还长。现在每个 hog 自己也看时钟，hold 到点就结束，不管受害者有没有轮上；
join 共用一个 20 秒预算，而不是各拿一个。单核加三个竞争进程实测：旧版 29.71s
（python 心跳滑了 24881ms），新版 14.43s（3578ms）；加到八倍超售仍是 25s 上下。
危害每一次都照常复现 —— 这才是这个例子存在的理由。

---

## 0.13.2（未发布）

一个 bug 修复。RPC 服务端的 listen backlog 是 `socketserver` 的默认值 5。

**一整队 worker 同时打第一通 RPC，大半会静默地等一秒。**

`_Server` 从来没设过 `request_queue_size`，于是 `listen(5)`。accept 队列满了之后
内核是**丢包**而不是拒绝，所以发起方不会收到错误，只是安静地等一次 SYN 重传 ——
起步就是一秒。64 个 peer 同时建首连，实测：

    backlog 5      p50 1013.70ms   最坏 2038ms   64 个里 55 个超过 100ms
    backlog 64     p50    6.01ms   最坏   12ms   0 个
    backlog 4096   p50    4.49ms   最坏    9ms   0 个

膝点正好落在"backlog ≥ 同时连上来的人数"上：accept 一次就要起一个线程，循环排空
不过来。所以任何固定值都会在某个规模上塌 —— 400 个 peer 撞 128 的 backlog 一样慢
（p50 1089.84ms，400 个里 268 个超过 100ms），要 4096 才平（p50 34.34ms）。

而"同时连上来多少人"就是集群有多少个 peer，这个进程无从知道，所以改成向内核要
`socket.SOMAXCONN`，最终由 `net.core.somaxconn` 封顶 —— 要多少由运维决定，而不是
由一个二十年前的默认值决定。

这条一直都在，只是要几十个 peer 同时启动才看得见；症状是启动慢，不是报错，所以
很容易被记到别的账上。

---

## 0.13.1

一个 bug 修复。取消一个 `achanges()` watcher 有可能不生效。

**取消和铃声撞在同一轮 loop 迭代里时，取消会被吞掉。**

异步 watcher 等的是 `_LoopBell.wait()`，而它等的方式是 `asyncio.wait_for`。
3.11 的 `wait_for` 在等待者被取消时有这么一句：

    except CancelledError:
        if fut.done():
            return fut.result()

心跳的字节刚把 future 置位、`cancel()` 紧接着落在同一轮回调里，这句就把
CancelledError 当成"结果已经到了"直接吞了。watcher 若无其事地转下一圈，外面的
`await task` 永远不返回，任务停在 CANCELLING 再也出不来。压测里大约 24 轮命中
一次，每次都卡在同一个 watch 上。

改成直接 `await` 那个 future：取消落在任务真正挂起的那个 future 上，没有任何
中间层能把它转成一个普通答案；超时则由 `call_later` 置结果送达 —— 它本来就是
个普通答案，不该以异常的形式旅行。

复现是确定性的：把响铃和取消排成同一轮里的两个 `call_soon`，修之前必吞，修之后
必传。原来那条"超时不能以异常逃出去"的 mutant 换到了新写法上，因为超时已经根本
不走异常了。

对使用者的影响：`task.cancel()` 现在真的能停下一个 watcher。此前唯一可靠的停法
是显式 `watch.close()`，那条路一直是对的，也仍然推荐 —— 它是同步的，哪怕 loop
再也不调度这个任务也照样生效。

---

## 0.13.0

又一整轮定期巡检。规矩没变：**先写脚本量，量到了再改**；每条新行为都要验证"把
bug 放回去，测试会变红"。这一版十六个 bug，没有一个来自推理。

这一版还多了两样以前没有的东西：**一套 benchmark**（十一个场景，带实测出来的
门禁容差），以及一个被它当场抓出来的事实 —— 有些"慢"根本不在代码里。

### 行为变化

**`flush()` 现在问"我发布的那份被收下了吗"，而不是数拍数。**

它原来等 `beats_ok + 2`。数拍数分不出"给我这份的 ack"和"给上一份的 ack"，只好
假设在途那拍是改动之前组好的，于是多等一拍 —— 而那一拍是驻留的，要挂满一个
hold，可 publish 刚刚才把驻留打断过。现在发布的状态带版本号，被**接受**的 ack
才记账。

    flush 紧跟 publish            645ms -> 41ms
    状态已经可见之后再 flush     1096ms -> 0.00ms

只有被接受的才算。拒绝是一次带 `accepted: false` 的普通回复 —— 座位被后来者拿走、
state 超尺寸、池形状不符 —— 注册中心**什么都没存**，所以它什么都没确认。

**心跳连接开了 `TCP_NODELAY`，每一拍不再付 40ms。**

上面那 41ms 不是 flush 的。实测 `update()` 到 ack：上一拍安顿好时 0.66ms，
**连发 41ms**，二十次里十九次落在 40.83–42.22ms 的窄带上 —— 固定停顿，不是抖动。
hyper 的 connector 默认不开 `TCP_NODELAY`，而一次心跳是"写-读-写"，第二次写要等
对端 40ms 的 delayed ACK。开了之后连发 **0.28ms**。

注册中心那侧**没有**跟着开：实测发布路径和 watch 唤醒都纹丝不动（0.28 vs 0.28，
51.49 vs 51.44），买不到东西就不加。

**`join(registry_url=)`：去哪个注册中心报到。**

压过 `TINYRAY_REGISTRY`。环境变量仍是常规通道（launcher 一次给所有 rank 设好），
这个参数是给用不了它的调用方 —— 嵌在别人进程里的库，为了配置一次调用去改
`os.environ`，改的是整个进程，而且比这次调用活得久。

它**选**一个注册中心，不**加**一个：一个进程一个成员一个注册中心，仍然没有故障
转移。给一串地址现在**当场拒绝** —— 文档以前把变量写成 `host:port,...`，那个逗号
会拼出 `http://a:1,b:2`，进程只报"注册中心没应答"，这话是真的但没用。

别和 `url=` 搞混：那个是"别人怎么找到我"，这个是"我去找谁"。

**参数装不进签名时，`*args` 和 `**kwargs` 也按注解转换了。**

`gather("1","2","3")` 配 `*shards: int` 原来到方法手里是 `(1, '2', '3')` ——
只转了第一个；`**kw: int` 完全不转。**半转是最坏的答案**，因为它看起来像是生效了。

**一个装不下的信封现在是 `TypeError`（422），不再是"可能已经执行了"。**

`{"args": 5}` 会让 `list(5)` 抛出一个裸 `TypeError`，两层 except 都接不住，
最后成了 HTTP 500 —— 而调用方把 ≥500 读作"它可能整个跑过了"。可这次请求的参数
**根本没被解开**。

**`scan()` 不再把类当成远程方法发布。**

嵌套类、枚举、`helper_cls = Helper` 这类类属性，原来全都出现在**随每一拍心跳发送**
的方法表里。原来的判据是"挂在类上的东西可调用" —— 而类本身就是可调用的。

### 修复

**丢包链路上，`join()` 不再把预算等满。**

首拍是同步的，但后台循环和它是**并发**的 —— 所以"已注册"可能由首拍完全不知道的
那一拍达成。实测 40% 丢包、`join(timeout=30)` 六个种子：循环在 **0.01s** 就拿到
ack，首拍照样等满 5s 预算，六次里占三次。首拍存在的唯一理由就是"返回时已在册"，
条件满足了就没什么可等的。端到端中位 **5.0s → 1.75s**，最差 12.5s → 4.26s。

首拍的预算切片（`min(timeout, 5s)`）保留了，但理由变了：它释放 GIL 阻塞，所以
它拿多久就是 **Ctrl-C 最坏被忽略多久**。实测链路黑掉、`timeout=60`、2s 后 SIGINT
—— 有切片 3.07s 退出，整份预算要 58.06s。

**连上却不说话的连接，会永久占住一个线程。** 声明了 body 却不发的调用方，能把
线程一直占到它自己关闭为止。实测 500 条这样的连接稳定在 125 个线程，而普通调用
全程 1.7ms 照常应答；声明一个 TB 的 content-length 也不会让 RSS 动一下。

**注册中心会回答一个它从未发出过的游标位置。** 客户端拿着重启前的版本号来问，
空日志让"还在窗口内"这个判断说了是，于是回"没有变化" —— 客户端就此抱着上一世的
名册，而且没有任何报错。实测：从 version+500 问起，那个池子**整个没出现在应答里**。

**fork 出来的子进程带着父进程的退出钩子。** 子进程退出时把**父进程的座位**注销了。

**`since=` 和 `fields=` 一起用会吞掉中间的变化。**

**过滤器会匹配它没有在问的整数**（含嵌套数字）。

**`wait()` 报不出座位丢了**；`join(timeout=)` 不是上界（会溢出）。

**注册中心 CLI 的一个操作员笔误直接抛 traceback。**

**围栏令牌有四种拼法。** 任何一处漂移，都会让一个被顶替的成员通过那道本该拦住
它的检查。

### 新增

**`bench.py`：十一个场景，49 秒跑完，门禁容差是量出来的。**

以前没有 benchmark，只有十几条延迟/速率的**上界断言**和贴在文档里的手工数字 ——
没有任何东西会说"这次改动让它变慢了"。场景是按"这里退化了会不会看不见"选的，
不是按"这里有个 API"。每个场景都做特性探测，所以同一个脚本能跑在老 wheel 上，
跨版本比较用的是同一份代码。

容差 20% 是量出来的：五次跑，对中位数的最坏偏差。被门禁盯着的指标在 0.2–1.1%；
被排除的是自己就会动的那些（`join_cold_start` p50 **2306%**，双峰；各种 `max_ms`
24–34%）。两个方向都验证过：四次独立跑 16/16 静默，注入 25% / 2× / 10.6× / −30%
的退化全部报出。

它当场抓到三个我原本相信的错数字，其中一个是我自己刚写下的 40 倍"提升" ——
`join_cold_start` 的 p50 在 n=10 时摆动在 1.0/21.2/41.6ms，而 n=40 下
v0.12.0 是 25/40 快、HEAD 是 24/40，**完全一样**。分布是双峰的，中位数是噪声。


---

## 0.12.0

一整轮定期巡检的产物。规矩没变：先写脚本量，量到了再改；改完必须验证"把 bug
放回去，测试会变红"。这一版里十一个 bug 全是这么找出来的，没有一个来自推理。

主题是**资源没还回去**和**契约说反话**。前者在 fork、事件循环、线程退出这些
边界上反复出现；后者是异常类型告诉调用方的事情与实际相反。

### 行为变化

**没读完的请求不再是"可能已经执行了"。**

`NotDelivered` 和 `OutcomeUnknown` 的区别是调用方**敢不敢原样重发**。服务端在
读完请求之前放弃的每一条路，原来都掉进"可能跑过了"的兜底：content-length 读不懂
（400）、body 发到一半停住（408）。用带计数器的真实 callee 量过，这些情况下方法
被调用 **0 次**。也就是说客户端告诉调用方的是反话，本来可以随便重发的非幂等调用
被迫当成不安全。大 payload 撞上忙链路、body 传一半停住是很平常的事。

**参数装不进签名，现在是 `TypeError`，不再是 `RemoteError`。**

调用参数有五种错法，原来只有"类型不对"说了实话。位置参数太多、少给必填、关键字
名字不认识、同一个参数给两次，四种都回 `RemoteError` —— 那说的是"方法跑了并且
抛了异常"，可它一次都没跑，`except TypeError` 也接不住。没有注解的方法更彻底：
整段检查被跳过，任何形状的参数都直接送进去。

改法是拿签名 `bind()` 一次，而且**比原来更便宜**：注解和签名本来每次调用都重算
（3.63 µs 和 11.26 µs），存下来之后 bind 只要 2.29 µs，`_coerce()` 从 20.05 µs
降到 4.58 µs。

**名字和世界大小现在会当场拒绝。**

非 ASCII 的池名、方法名、request id 一律在选定它的地方报错。先试过让它们能用
（把请求头改成 UTF-8 字节），但拒绝的写法比编码的写法**少 12 行**，而且方法名
是要进 URL 的 —— `a/b` 和 `a?b` 原来"能用"纯属服务端逐字读路径的巧合。

`WORLD_SIZE=0` 原来静默通过：声明零个座位的池子会让 `epoch()` 冻结在"当下在场
的人"身上（实测一个孤零零的成员 82ms 就冻结了一轮）。`slot=9, size=4` 也照收，
`RANK=-1` 会漏出 `OverflowError`。现在都拒绝，并且报错点名是哪个环境变量。

### 修复

**事件循环关掉之后，它的铃没有还回去。** `_LoopBell` 持有自己 loop 的强引用，
所以清理时检查的那个弱引用永远不会死 —— 那条分支根本触发不了。每个用过 watch 的
`asyncio.run()` 都留下一个铃和一根管道：101 次之后 **210 个文件描述符**，而且
心跳每一拍都要往那 101 个死管道各写一次。

**两个线程会关掉同一个管道。** 回收旧铃是"下一个来问的线程"顺手做的，于是两个
线程同时走"先列出来再逐个关"那段。`os.close()` 调两次，而第一次释放的号码内核会
立刻发给下一个要 fd 的人。实测 8 线程各 25 轮 `asyncio.run()`，**4 个线程在第 1
轮就死**（`EBADF`）。EBADF 只是能看见的那一面。

**fork 之后子进程带着父进程的连接。** 两条路都有，同步那条更坏：父子在同一条
keep-alive 连接上轮流发请求，**应答按到达顺序发错了人** —— 父进程 `want 'P118'`
拿到 `'C0'`，同时子进程 `want 'C0'` 拿到 `'P118'`。没有异常、没有警告。异步那条
表现为 `HTTP 400`（`OutcomeUnknown`，这次调用可能已经执行了）或 `FileExistsError`
（两个进程往同一个 epoll 注册同一个 fd）。

**fork 时锁正被别的线程持有。** fork 复制锁但不复制持锁的线程，子进程继承一把
永远锁着的锁：实测第一次用 watch 就**永久卡死**（5 秒后只能 kill），修好之后是
0.10 秒。

**问"谁接手了"会错过已经发生的换人。** 典型顺序就是这样：一次调用撞上 `Fenced`，
**然后**才去问新任期是谁。`wait_replacement()` 和 `await_replacement()` 都只订阅
"接下来的变化"，从不先看一眼当下已经成立没有 —— 换人如果已经完成，两个都坐满整个
超时然后返回 `None`，5/5 复现。（这个 bug 难看见：随后任何一条无关变化都会让它
蒙对，所以第一次测量时同步版答对了，害我以为只有异步版坏。）

**async 方法被交给一个已经停转的事件循环。** `serves=` 的成员在 `asyncio.run()`
块结束之后还活着，循环却不转了；而 `run_coroutine_threadsafe(...).result()` 没有
超时。实测 `max_concurrency=2`：**两次 async 调用就把两个槽位永久占死**，之后这个
成员对任何调用都答 `at its concurrency limit`，**连同步方法也一样** —— 而它还在
注册、还在心跳、还在广告地址。循环被关掉那种则谎称"你的方法抛了 Event loop is
closed"，其实方法压根没跑。判断依据改成"现在转不转"。

**走了不放手。** 被服务的那个对象十有八九是个模型或一份数据，三样东西各自攥着它：
`atexit`（`join()` 交给它的 `member.leave` 从不被注销，成员被钉到进程结束）；
已经停在 keep-alive 连接上的**处理线程**（关掉监听 socket 碰不到它，什么时候放手
取决于调用方）；以及注解缓存拿绑定方法当键（绑定方法带着实例）。实测 30 轮
join/调用/leave 留下 **30 条线程、30 个方法服务器、60 个文件描述符**，而描述符
默认上限是 1024 —— "一份工作换一份工作"的进程正是这个形状。三处都修，全部归零。

**拿到答案的那次调用，`stats()` 里可能还没有它。** 计数原来发生在应答**写出去
之后** —— 服务端先把字节推上 socket，再更新计数器。于是调用方先拿到答案、再读
`stats()`，读到的数字可能还没算上自己：实测普通调用 0.2%、抛异常的 0.8%。对断言
它的东西是抛硬币，对读它的东西是个错数。`calls`/`failed` 挪到写之前；`in_flight`
和 `busy_ns` 留在写之后，因为它们问的是"有几个 handler 在跑、跑了多久"，而写一个
16 MiB 的应答确实占着那条线程。顺带把答案的组装和发送分开了：`_dispatch()` 现在
只负责想出该答什么，写只有一处而不是四处。

**没读完的请求会把下一个请求带歪。** 服务端放弃之后就不再读，可字节还在路上。
留在连接里，服务端会拿它们当**下一个请求的请求行**去解析：实测回过 408 之后，同一
条连接上又被推下来一个 `HTTP 500`，客户端复用长连接时会把它当成自己下一个请求的
答复。三个入口（body 半截、长度读不懂、长度是负数）都关掉连接。

### 内部

新增资源清理回归测试（现位于 `tests/membership/test_cleanup.py`）：一条可复用的判据 —— join、做该做的事、
leave 之后，进程和注册中心两侧数得出来的东西都得回到原处。它当场抓到了上面那个
线程泄漏。盯的是计数不是 RSS：注册中心 500 轮的 RSS 增量按批是
3.00 / 0.24 / 0.44 / 0.04 / 0.24 kB，那是日志涨到上限加分配器，断言它只会得到
一条会飘的测试。

变异条目从 52 增到 79。修了变异检查自己的一个 bug：两个把同一文件缩短同样字节数
的变异体在同一秒内写入，字节码缓存（键是 mtime 秒 + 文件大小）会让第二个跑第一个
的代码 —— 实测变异体已在磁盘上而 `import tinyray` 拿到的仍是未变异的类。

这一轮还翻出 8 条没有牙的测试，最后一条是判据自己的第一版：它等 2.5 秒再看，而
租约是 2 秒，足够清扫器把任何东西收拾干净 —— 把整个"说再见"分支关掉都照样全绿。

### 要改什么

- 接 `OutcomeUnknown` 来处理"可能已执行"的地方：400 和 408 现在是 `NotDelivered`。
  这是放宽 —— 原来必须带幂等键重试的，现在可以直接重发。
- 接 `RemoteError` 来判断"远端方法失败"的地方：参数装不进签名现在是 `TypeError`。
  如果你在 `except RemoteError` 里统计业务失败率，这部分会从那里移走。
- 非 ASCII 的池名、方法名、request id 现在抛 `ValueError`。
- `WORLD_SIZE=0`、`slot >= size`、负的 `RANK` 现在抛错而不是继续。

---

## 0.11.0

上一轮 API 使用方提的七条里做掉六条，另外五个 bug 是在此后的定期巡检里翻出来的。
巡检的规矩是：先写脚本量，量到了再改；改完必须验证"把 bug 放回去，测试会变红"。

### 新增

**`pool.until(predicate)` / `auntil()`** —— 等条件的通用底座。

每个手写的等待循环都要做对同样四件事：先看已经成立没有、把 revision 无缝交接
过去、被 `close()` 时停下、`Fenced` 放出去而不是当成"条件还没满足"。第二件做错
最难发现 —— 池子在"先看一眼"和"开始订阅"之间动了，等待就会为一个立刻成立的条件
白等满整个超时。下面两个都是它的特例。

**`await apool.await_ready(count, timeout, **filt)`** —— `Pool.wait()` 的事件
循环版。`AsyncPool` 原先继承的是同步阻塞版，它在 loop 上不是"不够优雅"，是
**停掉整个 loop**：实测一秒里只放过 **5** 次 10ms 的 tick，本该有一百次。
用 `asyncio.to_thread` 包一层也不对 —— 取消它并不停掉底下的线程，整段等待都占着
默认 executor 的一个 worker。

**`pool.wait_departure(identity)` / `await_departure()`** —— 等指定任期离场。
和 `wait_replacement()` 是两个问题：后者只在有人接任时才回答，前任只是走了、没人
接手的话它会等满超时返回 `None`。要接手工作的一方通常只需要知道前任不在了。

**只盯几个字段的 watch**：

```python
pool.changes(fields=["role", "ready"])
```

给了 `fields` 之后，只有这些字段（或成员进出、座位换人）真的动了才产出快照。
比较发生在 **Rust 缓存里**，在任何东西被序列化之前 —— 5,000 成员实测
`snapshot()` **8.78ms** 对 `field_digest()` **0.40ms**，**22 倍**。放在 Python
层做 predicate 是省不下来的：它要拿到 `Snapshot` 才能判断，那时钱已经花完了。

**`with tinyray.request_id("commit-42")`** —— 让重试共用一个名字。自动生成的
id 每次都变，对追踪是对的、对幂等是错的；没有这个的话幂等 key 只能当普通参数
一路传下去。用块而不是逐调用参数，因为重试本来就是块的形状，而关键字会和被调方
自己的参数名打架。ContextVar 实现，跟着 `await` 走进这个块起的任务，不漏进邻居。

**`stats()` 增加服务端与观察计数**：`calls` / `failed` / `refused` /
`in_flight` / `peak_in_flight` / `busy_ms` / `concurrency_limit`（只有传了
`serves=` 才有），以及 `watch_wakeups` / `state_bytes` / `pool_revision` /
`watched_pools` / `short_polls`。

这一半存在，是为了让"要不要给长调用单开一条通道"有答案而不是有观点。
`max_concurrency` 挡的是无限堆积，**不是隔离**：槽位占满之后 control 调用一样
吃 503。`refused` 和 `peak_in_flight` 摆在一起看就知道是不是正在发生。

### 修复

**被服务对象的 property 会在发现方法时被求值。** `getattr` 会执行描述符，而这个
领域的服务对象满是 `device`、`model`、`step`。三种后果都实测过：有副作用的被触发
一次；**会抛的那种直接让 `join(serves=...)` 失败**，报出 `RuntimeError: no GPU
on this box` —— 一次应用根本没发起的调用；返回 callable 的被登记成了远程方法。
现在问类不问实例。

**`TINYRAY_ADVERTISE` 写成 `http://...` 会拼出坏地址却注册成功。** 六种人们实际
会写的形式**四种是坏的**：`http://10.0.0.5` 拼成
`http://http://10.0.0.5:33097`，`10.0.0.5:8080` 拼成 `http://10.0.0.5:8080:33097`。
四种都注册成功，失败发生在别的进程、别的时刻。现在当场拒绝并说明正确写法；前后
空白无歧义，直接修掉。

**`CallContext` 只在参数排最后时才工作。** 放第一位或中间、调用方传位置参数就报
`Expected CallContext, got int` —— 调用方根本不为注入的参数发值，位置对不上了。
而这句话在怪调用方，问题却在被调方的签名顺序。现在按名传参，任何顺序都成立。
多传一个参数改走 422 通道，调用方拿到 `TypeError` 而不是
`OutcomeUnknown`（"可能已经执行过"）—— 什么都没跑过。

**超大响应的告警指向 asyncio 内部。** `stacklevel` 写死为 4，同步方向对、异步
方向错（协程跑起来时 `BoundMethod.__call__` 早已返回）。实测指向
`asyncio/events.py:84`。而 `warnings` 按位置去重，于是所有异步超大告警折叠成一条
被过滤掉 —— 第二次就彻底静默。现在向上走到第一个不属于 tinyray 的栈帧。

**`Epoch` 和 `Snapshot` 说是冻结，其实是可变列表。** 实测
`epoch.members.append(...)` 会让 `len(epoch)` 从 3 变 4。这份名单发给每个 rank 建
同一个进程组，任何一次就地 `sort()` 都能让各 rank 分道扬镳 —— **从这个类型本身
走进它要防的死锁**。现在交出元组。

### 内部

`Handle` 和 `AsyncHandle` 的方法查找合并成一份（原先 6 行里 5 行逐字相同）。
新增 `lines.py` 统计代码行数（注释单列，它们装的是决策背后的实测数字）。
变异条目从 22 增到 52，注册表状态机和客户端缓存首次纳入。

### 要改什么

- `Epoch.members` / `Snapshot.members` 现在是元组。`snap.members == [...]` 这样
  和列表字面量比较的地方要改成比较内容。
- `TINYRAY_ADVERTISE` 带 scheme 或端口的现在会抛 `ValueError` 而不是静默拼错。
- 被服务对象上返回 callable 的 property 不再作为远程方法发布。

---

## 0.10.0

### 行为变化

**变化流在本进程被顶替时抛 `Fenced`，不再安静结束。**

流本来有三种结束方式 —— 超时到了、有人 `close()` 了、本进程丢了座位 —— 而三种
在消费者眼里长得一模一样：循环安静地退出。

前两种是"没事了"，第三种是"出事了"：座位没了，本地缓存从此**冻结**，之后每一次
查询都是陈旧的却不声张。混在一起的代价是，唯一的区分办法变成事后去查
`Member.accepted` —— 正是不该让调用方去猜的东西。

```python
try:
    for snap in pool.changes():
        ...
except tinyray.Fenced:
    # 座位没了。停掉 GPU、推理服务、自己开的 socket，然后重新 join。
```

同步和异步两侧行为一致。`close()` 和超时**仍然**安静结束 —— 只有真出事的那一种
会抛。

这是这个项目里反复出现的同一条取舍：`NotDelivered` 和 `OutcomeUnknown` 分开，是
因为一个能盲目重试、一个不能。同理，"我不想看了"和"我没资格看了"不该用同一种
方式结束。

**要改什么**：如果你现在靠"循环退出"当作正常收尾，加一个 `except tinyray.Fenced`。
如果你本来就在循环后面查 `Member.accepted`，那段可以删了。

---

## 0.9.1

两个 bug，都是"异步侧和同步侧行为不一致"，都是在做代码去重时翻出来的。

### 修复

**`achanges(timeout=)` 会抛 `TimeoutError`，而 `changes(timeout=)` 只是结束。**

异步侧等的是管道，底下用 `asyncio.wait_for`，超时会抛；这个异常被一路放了出去。
时间用完是一个**答案**，不是失败 —— 调用方回到循环里重新检查自己真正关心的东西，
同步侧的 `wait_revision` 一直就是这么用的。两边现在一致了。

这条 0.9.0 上就有。

**`await_fenced()` 还在用 `asyncio.to_thread`。**

0.8.0 把 `achanges()` 从 executor 线程搬走时，它被落下了 —— 因为**它一直没有任何
测试**。取消一个 `await_fenced()` 并不会停掉底下那个线程。实测：取消 40 个之后，
紧接着一次 `asyncio.to_thread` 要等 **5 秒**；现在是 0。

现在 `python/tinyray/` 里已经没有任何 `asyncio.to_thread`。

### 内部

八个等待各自抄了一遍同样的截止时间算术（八次写错的机会），两个
`wait_replacement` 共用十二行逐字相同的参数解析 —— 那是能在一处修好、另一处忘掉
的十二行。都收进了一个地方。上面两个 bug 就是这么翻出来的。

> 补上的那条 `await_fenced` 测试，第一版在把 bug 放回去时是**挂死**而不是失败：
> 无界等待让解释器退出时去等那些永不返回的线程。挂死的测试比没有测试更糟 ——
> CI 只会报超时，不会告诉你哪里错了。已改成有界，三个变异体全部抓得住。

---

## 0.9.0

### 新增

**注册中心自报能做什么，不用再靠属性探测去猜。**

```python
me.registry                          # -> RegistryInfo
me.registry.protocol                 # 只增不减；老到不报的读作 0
me.registry.version                  # 对面的版本号，写日志用
me.registry.supports("long_poll")    # 要分支就看这个
```

```console
$ curl -s http://registry:7000/health
{"status":"ok","version":"0.9.0","protocol":1}
```

猜是猜不出来的：老注册中心对长轮询请求的回答**又快又对**，只是不挂起，所以
"挂起了但什么都没发生"和"根本不会挂起"从客户端看完全一样。实测对着 0.6.1 的
注册中心：每秒 **14.5** 次请求，当前版本 **0.12** 次 —— 一百倍，而 `/health`
只说 `{"status":"ok"}`，客户端也没有任何入口可问。

`tinyray.__version__` 说的是本地这一侧。注册中心是另一个进程，可以独立升级，
所以那是另一个问题，现在有了另一个答案。

**版本不匹配时 `join()` 会说话**，发一条 `OldRegistryWarning`。因为这是性能悬崖
不是报错：一切照常工作，只是变化要等一个心跳间隔而不是一个往返，而且请求量翻
几十倍。除了这条告警，没有任何东西会提。照常用 `warnings.filterwarnings` 关掉。

功能名写错抛 `ValueError`，不返回 `False` —— 后者会让一个笔误安静地走进降级分支，
而那正是这套机制要消灭的东西。

对照表 `RegistryInfo.FEATURES` 放在依赖它的这一侧，所以老客户端不必认识将来的
功能。今天只有一个条目（`long_poll` 需要 protocol 1），这是实话：注册中心侧对
客户端可见的能力变化，到目前为止只有长轮询这一件。

### 兼容性

`BeatAck` 的两个新字段都带 serde 默认值，缺失读作 `protocol=0` / 空版本号，所以
升级过程中新客户端配旧注册中心只会收到告警，不会失败。这条由
`crates/tinyray-proto/tests/wire.rs` 守着。

---

## 0.8.1

**0.8.0 从未上架 PyPI** —— 和 0.7.0 一样，流水线在 `lint and test` 就失败了。
tag 留着，那个版本号在 PyPI 上不存在；要 0.8.0 的内容请装 0.8.1。

### 修复

**被顶替的成员要 940ms 才知道自己是幽灵，本该 1-2ms。**

这是 0.8.0 引入的回归，而且踩在围栏上。围栏有两道关：调用方带的 identity 挡
"地址被后一任复用"，两代跑在不同端口时挡不住；剩下的全靠旧进程自己知道自己
出局了。慢 940ms，就意味着这段时间里它会正常应答旧句柄。

原因是 `hold == 0` 在心跳循环里身兼两义：既表示"注册中心老得不会挂起"，也表示
0.8.0 新加的"取消之后顶上来的那一个不挂起"。完成一拍后的分支按前者理解，去睡满
一个 interval —— 而且睡的时候不再挂起，注册中心叫不醒它。现在这个判断读的是
循环的**意图**（`shared.hold_ms`），不是上一个请求碰巧要了什么。

是 `examples/06_fencing.py` 在双核上撞出来的（5 次里失败 4 次；v0.7.1 同样条件
0 次）。已补一条测试直接钉住这个延迟 —— 把 bug 放回去会量到 4,594ms 对 500ms 的
上界。**第一版测试没有牙**：它等得太久，错过了那段错误睡眠的窗口，把 bug 放回去
照样通过，重写后才抓得住。

---

## 0.8.0

按 API 使用方（rl-bridge）的十条请求逐条核过之后的一轮。**接受六条、拒绝四条**，
判断依据都是实测数字，不是论证。拒绝的理由写在 `docs/api.md` 里对应的位置，
省得下次再问一遍。

### 新增

**状态发布和就绪声明分开了。**

```python
me.update(**state)      # 只发布状态，不碰就绪
me.replace(state)       # 整体替换，不碰就绪
me.is_ready             # 此刻对外宣称的就绪状态
```

以前只有 `ready()` / `set_ready()`，两者都硬编码 `ready=True`，而成员 API 里
没有任何只更新状态的路径。后果不需要竞态就能看到：`unready()` 之后一句
`ready(step=1)`，对端看到的 ready 就从 `False` 变回 `True` —— 一个只想上报进度
的组件，把另一处刚下的暂停静默掀掉了。

分开之后，"每个 Member 只有一个 readiness owner" 不再是要靠自觉遵守的约定：
其余代码调用 `update()`，结构上就碰不到就绪位。

**watcher 能停下来了。**

```python
w = pool.changes()      # 现在返回 Watch，可迭代、可关闭、也是上下文管理器
w.close()               # 可以从另一个线程或任务关掉
with pool.changes() as w: ...
```

以前 `changes()` 返回生成器，阻塞在事件上而不是停在 `yield` 上，所以别的线程既
`close()` 不了它，设标志位它也看不见。实测：一个非 daemon 线程在里面跑，进程再
也退不出来，`leave()` 也解不开。现在 `leave()` 会关掉所有还活着的 watcher。

**`achanges()` 不再占用 executor 线程。** 改为等在一个心跳会写入的管道上，由事件
循环 select。取消 40 个 watcher 之后再要一个 `asyncio.to_thread`：**3,092ms →
1ms**（24 核机器，默认 executor 28 个 worker 曾经全被卡住）。

**`pool.wait_replacement(slot=/identity=)`** 和异步的 `await_replacement()`：
阻塞到座位由另一个任期接管，返回接任者。`Member.wait_fenced()` 是同一个问题的
自视角，这个是旁观视角。

**每次调用带 `request_id`**，在 `CallContext` 上可读，两侧日志能指着同一次尝试
说话。**不做幂等缓存** —— 被调方无从知道重放是否安全，那个决定属于调用方，
`NotDelivered` / `OutcomeUnknown` 的区分就是为了让它做得出来。

### 修复

**发布得太快会把自己的心跳饿死。** 发布会取消挂起的请求，好让新状态立刻出去。
但顶上来的那个请求又被挂起了 —— 于是下一次发布再把它取消掉，如此往复，一拍都
完不成。实测每秒 379,000 次发布之下，注册表在第 4 秒把这个还在不停说话的成员
**从它自己的池子里删掉了**，而 `beats_ok` 全程停在 0。

修法是：**取消之后顶上来的那个请求不挂起**。它本来就带着新东西，没什么可等的，
一个 RTT 就回来了，状态随即落地。代价是一次往返，不是一整个 hold。

取消路径上另外补了 `MIN_GAP`，把风暴下的请求速率从每秒 24 次压到 11 次。它是
速率上界，**不是**防饿死的那一环 —— 这点实测过，两者互不相干。

> 中途试过一套"发布序号"，用来分辨"取消的是不是一个已经带上这次发布的请求"。
> 实测下来带与不带的数字**一模一样**（5/s 时每次发布 1.00 个请求，50/s 时
> 0.23），所以整套删掉了。留着就是一个没人守得住、也没买到任何东西的分支。

**同值发布不再花代价。** state 和就绪位都和已发布的逐字节相同时，既不敲醒心跳、
也不抬高池子版本。注册表侧本来就不抬版本，客户端侧过去仍会白花一次请求。

**state 和就绪位现在在同一把锁下发布。** `compose()` 过去在 Mutex 里读 state，却
把 ready 当独立原子量读，一次心跳可能带着新 state 和旧就绪位出去。没能复现出来，
但 `ready(**kw)` 的语义就是这一对，所以锁的也该是这一对。

**`ready()` 的合并加了锁。** 合并是读-改-写，两个发布者撞上会丢掉彼此的 key。
今天打不中 —— `MAX_STATE` 把窗口卡得很窄，8 个发布者 300 轮一个没丢，而把窗口
撑开 0.5ms 就丢了 420 个。防的是窗口以后变宽，不是今天的 bug。

### 明确不做

| 请求 | 为什么不做 |
|---|---|
| `hold_ms` 独立暴露 | 它不决定新鲜度。ttl=20s 下发现延迟 41ms，因为变化是**唤醒**挂起的请求，不是等它超时。hold 只管空闲请求率和黑洞连接的察觉速度，暴露出去只会让人为了"更快"把它调小，延迟一点不省而请求量翻几十倍 |
| 服务端状态投影 / predicate | 它真正解决的是"被无关变更吵醒"，不是带宽，而 state 已有 16 KB 上限。需要成员数、state 大小、无关唤醒占比三个真实数字才谈得上，否则做出来大概率是错的抽象 |
| 独立 Queue / streaming 通道 | 同一个 leased-queue 论证已经否过（见 `docs/02-design.md`）。`max_concurrency` 解决的是**无限堆积**，不是隔离：它在 dispatch 之前返回 503，所以拒绝是安全可重试的，但并发槽被长驻 RPC 占满时，control 调用同样吃 503 —— 控制面并没有和数据面分开。目前使用方不跑共享 Actor 的长驻 RPC，所以这条先不做，一旦要跑就得重开 |
| 跨 pool 一致性快照 / 原子 fanout | 每个成员是独立到期的租约，跨 pool"一致"只对注册表账本成立、对现实不成立。原子 fanout 是共识，另一个系统的问题 |
| readiness generation / CAS | 层次错了。线上不存在这个乱序：只有一个心跳循环、顺序发送、注册表存最后值。乱序只在进程内，而那是 `update()` 解决的 |

---

## 0.7.1

**0.7.0 从未上架 PyPI** —— 它的流水线在 `lint and test` 就失败了，wheel 和发布
全部跳过。tag 和 GitHub Release 留着，但那个版本号在 PyPI 上不存在；要 0.7.0 的
内容请装 0.7.1。

### 修复（测试，不是产品）

拦下 0.7.0 的是 `test_a_lossy_link_still_lets_a_member_start`：40% 丢包下跑五次
启动，**零容忍**。而下面那条 slow 测试早就量过——30 秒截止时间下单次启动仍有约
0.8% 会失败，没有归零。五次零容忍等于每跑一次套件约 4% 概率乱叫。

这次它乱叫在了发布上。改成五个种子跑在一条测试里、容忍一次，和那条 slow 测试
同一个做法、同一组数字。把丢包率拉到 95% 它照样变红，判别力还在。

**不是长轮询的回归**，这一点查过：`hold_ms` 初值为 0，只在一次成功 ack 之后才
设置，所以在一拍都没落地的丢包场景下，超时仍是旧的 `interval × 3/4`——join 这
条路径完全没被 0.7.0 碰到。本地 60 次启动零失败也印证了。

---

## 0.7.0

**`ttl_ms` 不再决定变化多久可见。**

原先它一个数管三件事：租约长度、心跳间隔（`ttl/4`）、以及发现延迟的上界（也是
`ttl/4`）。万卡规模下这两个诉求直接打架 —— 调大免得把 GC 停顿判成失联，调小才
能让事件驱动及时。

现在注册中心在没话说的时候**把应答挂起**，池子一动立刻返回。

| ttl | 旧的发现延迟上界 | 实测发现延迟 | 心跳/秒 |
|---|---|---|---|
| 2 s | 500 ms | 21 ms | 2.06 |
| 8 s | 2,000 ms | 19 ms | 0.75 |
| 20 s（默认） | 5,000 ms | **21 ms** | 0.44 |

**默认配置下快了 238 倍，而心跳数量一次都没多。** 自己发布状态仍是即时的
（实测 0.6 毫秒）。

40,000 成员静默态：固定间隔轮询 163 MiB / **622% CPU**，长轮询 325 MiB /
**20.5% CPU** —— 约 4 KB 一条挂起请求，CPU 差 30 倍，因为那些请求原本都在空转。

### 不用改任何配置

**没有新旋钮。** 客户端请求挂起的时长，就是它本来要睡的那个间隔。`ttl_ms` 仍是
唯一的数，只是它现在只管失联判定和流量。

**双向兼容。** `hold_ms` 在 Beat 里而不是只由服务端定：早于这个字段的客户端不发
它，服务端视为 0 立刻应答，行为和以前完全一样；新服务端也不会把旧客户端挂到超时。

### 修复

**`beat_timeout` 的余量是个常数，在短租约下顶过了租约本身。** `hold + hold/4 +
2000ms` 在 ttl=2000 时是 2.6 秒 —— 比租约还长，于是一个丢包就足以掉出名册，而
「一拍必须有界」正是为了防这个。改成按比例，任何租约下都稳定在 0.44 个租约。

---

## 0.6.1

### 修复

**`changes()` 每一拍都产出快照，不管池子有没有动。** 它挂在客户端的拍数计数器
上，而那个计数器每次心跳都会前进 —— 实测 14 秒里产出 25 份快照，其中只有 4 份
是真的变化，84% 是空转。5000 成员时一份快照要 10.6ms，那些全是白重建。更要命的
是消费者还得自己 diff 才知道有没有变，这个 API 的意义基本被抵消掉。

现在铃照旧每拍响（`join`、`flush`、`wait_fenced` 等的是心跳结果），但产出由
**池子自己的 version** 决定。同样的场景：3 份产出，3 次真变化。

**`Snapshot.revision` 给的是客户端拍数，不是池子版本。** 文档承诺的是「每个 pool
有单调 revision」，拿到的却是一个客户端本地的、跨 pool 共享的计数器 —— 两个进程
拿到的数对不齐，`changes(since=)` 也就没法真正表达「从这个位置接着往下看」。
现在它就是池子的 version。

顺带：`snapshot()` 此前分两次读缓存（成员一次、版本一次），中间心跳可以落在当中。
现在和 `epoch()` 一样在一把锁内读完。

---

## 0.6.0

调用方要的三件事：**知道谁在调我、知道调用到底跑没跑、知道自己被换掉了**。
外加把库里最后一处轮询拆掉。

### 新增

**RPC 结果分成四类，而不是三类。**

```python
except tinyray.NotDelivered:    # 确实没送到 —— 方法一定没跑，直接重试
except tinyray.OutcomeUnknown:  # 可能跑了 —— 带同一个 request id 重试，或保证幂等
```

两者都是 `Unreachable` 的子类，既有的 `except Unreachable` 不受影响。

**调用方身份。** 方法签名里标一个 `tinyray.CallContext`，库会填上调用方的
identity / pool / slot / incarnation。调用方那边不用写任何东西。租约因此可以绑
到任期上，而不是绑到一个业务参数上 —— 后者会忘了传，也会传错。

**`Member.wait_fenced()` / `await_fenced()`。** 被更晚的任期顶替时主动醒来。
RPC 层本来就会拒绝打到旧成员的调用，但一个进程手里可能还攥着 GPU、推理服务和
自己的端口，它得被告知才能停掉那些。

**`Member.set_ready(state)` 与 `flush(timeout)`。** 前者整体替换 state；
`ready(**kw)` 是合并语义，所以在此之前**没有任何办法把一个 key 拿回来**。
`flush()` 阻塞到注册中心确实收下，省掉"发布完再反查自己"那一步。

**`Pool.snapshot()` 与 `Pool.changes()`。** 快照包含**入座但还没 ready** 的成员，
每条记录带 `incarnation` 和 `ready`，并带一个单调的 `revision`。`changes()`
阻塞在事件上产出新快照，池子不动就不返回。异步用 `AsyncPool.achanges()`。

给的是**快照流而不是事件流**，这是有意的：客户端以心跳为采样率，注册中心会把一个
间隔内的多次变化折叠成「该成员的当前状态」，所以「不丢事件」是协议兑现不了的承诺，
而「不丢状态」可以。事件是一次 diff 的事。

**`join(max_concurrency=)`。** 给同时执行的调用数封顶，超了返回 `NotDelivered`
而不是排队 —— 拒绝是有界的，排队不是。此前是每连接一个线程且无上限。

**`Member.identity`、`Snapshot.get(identity)`、`Pool.slot(k, require_ready=)`。**

**文档站** —— <https://hspk.github.io/tinyray/>，含上手指南与完整 API 参考。

### 变化

**库内不再有任何轮询。** `_settle`、`wait`、`epoch`、`join` 的首拍等待此前都是
sleep 循环，最紧的一处每秒 500 圈。现在全部阻塞在客户端的条件变量上，心跳落地
才醒。

一条测试直接读源码把这件事钉死 —— 运行时量不出来：实测空转 5 秒，事件驱动
0.0054s CPU、0.05 秒一圈的轮询 0.0109s，只差两倍且被机器噪声盖住，撑不起阈值。

**代码行数预算（2,000 行）不再是硬约束。** 它挡下过一次真的膨胀，但开始挡住
补窟窿的工作。约束换成 review 守，判据不变：*超出预算就是设计错了，不是写多了*。

### 修复

`Pool.changes()` 在成员被顶替后会永久阻塞 —— 心跳循环退出，铃不再响，消费者
等的是一件再也不会发生的事。现在流会有界结束。

---

## 0.5.0

八个静默失败。它们的共同点不是某个模块，而是**同一个事实有两份表示，却没有任何
东西强制它们一致**。

### 变化

**体积预算从「拒发」改成「警告」。** 超过 1 MB 的调用会发出
`tinyray.OversizeWarning` 然后照常送达。原先 `except ValueError` 包住大 payload
的代码将不再捕获到东西 —— 调用本身会成功。

在阈值上硬失败，会把一个从 900 KB 长到 1.1 MB 的返回值变成一次停机；而一次调用
是点对点的，超了只是两端慢。**注册中心的 state 预算仍是硬限**，那条不一样：
state 会复制给每一个订阅者，实测 6 MB 到 20 个订阅者变成 120 MB，它保护的是别人。

**`epoch()` 可能超时**，在过去会返回一轮的场景下。但过去返回的那一轮，其指纹
并不描述自己的成员名单 —— 各 rank 会一致认为「没问题」。

### 修复

**注册中心重启后，客户端会静默持有残缺名单。** 重启清空缓存是对的，但紧接着又
套用了同一个 ack 里的增量 delta，而那个 delta 是按**旧编号**算出来的。实测：两个
成员在册，客户端只持有一个，而且它的 roster 指纹**和服务端完全一致** —— 那正是
epoch 用来判断各 rank 是否一致的东西。

**告别会被「超时但仍送达」的心跳撤销。** 客户端超时只代表放弃等待，请求仍可能
被处理。实测在 200ms 租约下限：300 次 `leave()` 有 6 次成员仍在册，而告别本身
零失败。注册中心现在记住说过再见的任期一个租约周期。

**`epoch()` 冻结的指纹与名单不符。** 指纹按全部占位者算（ready 与否不影响它，
这是有意的），而冻结的名单按 ready 过滤 —— 于是两个 rank 可以拿到**同一个指纹、
不同的名单**，且两边都报告 valid。四路 churn 下实测 2,636,372 / 2,670,992 次
冻结对不上。

**`asyncio.run()` 每次泄漏一个 httpx 客户端和一个文件描述符。** 100 次调用 =
100 个条目、100 个 fd，而默认上限是 1024。缓存键 `id(loop)` 还是不安全的：地址
会被下一个循环复用，实测连续 5 次 `asyncio.run()` 有 2 次撞上已用过的 id。

**分块请求的 body 被静默丢弃。** 没有 content-length，于是长度被算成 0，body
留在 socket 里 —— `echo(x=7)` 当成 `echo()` 执行并返回 200，随后分块框架被当成
下一个请求行解析。现在明确拒绝并关闭连接。

**fork 出的子进程正常退出时永久挂死。** 继承来的 tokio runtime 在解释器收尾时
等一批不存在的线程，子进程卡在原生代码里，父进程的 waitpid 跟着挂。
`DataLoader(num_workers>0)` 就是这个形状。

**异常正文不受出口预算限制。** 一次 `raise ValueError("y" * 8MB)` 在网线上放了
16,777,461 字节，而同样大小的返回值会被拒掉。

**订阅超过 64 个 pool 会直接杀掉成员**（`accepted=false` 的含义是「座位没了，
停止心跳」，于是循环真的停了）。现在在客户端拒绝那次订阅并指出是哪个 pool。

### 测试

`cargo test` 在此之前**一个测试都没有**，现在有 50 个，覆盖注册中心状态机与线上
消息类型。17 个人为植入的回归全部被为它而写的那条测试抓到。

多机与规模已实测：三个容器跨网络命名空间互相发现并调用，指纹一致；10 万成员跑通
且订阅方看到全部，version 恰好 100,000；峰值 216,335 次/秒零错误。

---

## 0.4.1 及更早

见 `git log`。
