# Changelog

版本号按 [semver](https://semver.org/lang/zh-CN/)。**行为变了就是 minor**，
哪怕改动很小 —— 一个悄悄换了含义的异常比一个新函数危险得多。

---

## 0.9.1

两个 bug，都是"异步侧和同步侧行为不一致"，都是在做代码去重时翻出来的。

### 修复

**`achanges(timeout=)` 会抛 `TimeoutError`，而 `changes(timeout=)` 只是结束。**

异步侧等的是管道，底下用 `asyncio.wait_for`，超时会抛；这个异常被一路放了出去。
时间用完是一个**答案**，不是失败 —— 调用方回到循环里重新检查自己真正关心的东西，
同步侧的 `wait_revision` 一直就是这么用的。两边现在一致了。

这条 0.9.0 上就有。

**`await_fenced()` 还在用 `asyncio.to_thread`。**

0.8.0 把 `achanges()` 从 executor 线程搬走时，它被落下了 —— 因为**它一直没有任何
测试**。取消一个 `await_fenced()` 并不会停掉底下那个线程。实测：取消 40 个之后，
紧接着一次 `asyncio.to_thread` 要等 **5 秒**；现在是 0。

现在 `python/tinyray/` 里已经没有任何 `asyncio.to_thread`。

### 内部

八个等待各自抄了一遍同样的截止时间算术（八次写错的机会），两个
`wait_replacement` 共用十二行逐字相同的参数解析 —— 那是能在一处修好、另一处忘掉
的十二行。都收进了一个地方。上面两个 bug 就是这么翻出来的。

> 补上的那条 `await_fenced` 测试，第一版在把 bug 放回去时是**挂死**而不是失败：
> 无界等待让解释器退出时去等那些永不返回的线程。挂死的测试比没有测试更糟 ——
> CI 只会报超时，不会告诉你哪里错了。已改成有界，三个变异体全部抓得住。

---

## 0.9.0

### 新增

**注册中心自报能做什么，不用再靠属性探测去猜。**

```python
me.registry                          # -> RegistryInfo
me.registry.protocol                 # 只增不减；老到不报的读作 0
me.registry.version                  # 对面的版本号，写日志用
me.registry.supports("long_poll")    # 要分支就看这个
```

```console
$ curl -s http://registry:7000/health
{"status":"ok","version":"0.9.0","protocol":1}
```

猜是猜不出来的：老注册中心对长轮询请求的回答**又快又对**，只是不挂起，所以
"挂起了但什么都没发生"和"根本不会挂起"从客户端看完全一样。实测对着 0.6.1 的
注册中心：每秒 **14.5** 次请求，当前版本 **0.12** 次 —— 一百倍，而 `/health`
只说 `{"status":"ok"}`，客户端也没有任何入口可问。

`tinyray.__version__` 说的是本地这一侧。注册中心是另一个进程，可以独立升级，
所以那是另一个问题，现在有了另一个答案。

**版本不匹配时 `join()` 会说话**，发一条 `OldRegistryWarning`。因为这是性能悬崖
不是报错：一切照常工作，只是变化要等一个心跳间隔而不是一个往返，而且请求量翻
几十倍。除了这条告警，没有任何东西会提。照常用 `warnings.filterwarnings` 关掉。

功能名写错抛 `ValueError`，不返回 `False` —— 后者会让一个笔误安静地走进降级分支，
而那正是这套机制要消灭的东西。

对照表 `RegistryInfo.FEATURES` 放在依赖它的这一侧，所以老客户端不必认识将来的
功能。今天只有一个条目（`long_poll` 需要 protocol 1），这是实话：注册中心侧对
客户端可见的能力变化，到目前为止只有长轮询这一件。

### 兼容性

`BeatAck` 的两个新字段都带 serde 默认值，缺失读作 `protocol=0` / 空版本号，所以
升级过程中新客户端配旧注册中心只会收到告警，不会失败。这条由
`crates/tinyray-proto/tests/wire.rs` 守着。

---

## 0.8.1

**0.8.0 从未上架 PyPI** —— 和 0.7.0 一样，流水线在 `lint and test` 就失败了。
tag 留着，那个版本号在 PyPI 上不存在；要 0.8.0 的内容请装 0.8.1。

### 修复

**被顶替的成员要 940ms 才知道自己是幽灵，本该 1-2ms。**

这是 0.8.0 引入的回归，而且踩在围栏上。围栏有两道关：调用方带的 identity 挡
"地址被后一任复用"，两代跑在不同端口时挡不住；剩下的全靠旧进程自己知道自己
出局了。慢 940ms，就意味着这段时间里它会正常应答旧句柄。

原因是 `hold == 0` 在心跳循环里身兼两义：既表示"注册中心老得不会挂起"，也表示
0.8.0 新加的"取消之后顶上来的那一个不挂起"。完成一拍后的分支按前者理解，去睡满
一个 interval —— 而且睡的时候不再挂起，注册中心叫不醒它。现在这个判断读的是
循环的**意图**（`shared.hold_ms`），不是上一个请求碰巧要了什么。

是 `examples/06_fencing.py` 在双核上撞出来的（5 次里失败 4 次；v0.7.1 同样条件
0 次）。已补一条测试直接钉住这个延迟 —— 把 bug 放回去会量到 4,594ms 对 500ms 的
上界。**第一版测试没有牙**：它等得太久，错过了那段错误睡眠的窗口，把 bug 放回去
照样通过，重写后才抓得住。

---

## 0.8.0

按 API 使用方（rl-bridge）的十条请求逐条核过之后的一轮。**接受六条、拒绝四条**，
判断依据都是实测数字，不是论证。拒绝的理由写在 `docs/api.md` 里对应的位置，
省得下次再问一遍。

### 新增

**状态发布和就绪声明分开了。**

```python
me.update(**state)      # 只发布状态，不碰就绪
me.replace(state)       # 整体替换，不碰就绪
me.is_ready             # 此刻对外宣称的就绪状态
```

以前只有 `ready()` / `set_ready()`，两者都硬编码 `ready=True`，而成员 API 里
没有任何只更新状态的路径。后果不需要竞态就能看到：`unready()` 之后一句
`ready(step=1)`，对端看到的 ready 就从 `False` 变回 `True` —— 一个只想上报进度
的组件，把另一处刚下的暂停静默掀掉了。

分开之后，"每个 Member 只有一个 readiness owner" 不再是要靠自觉遵守的约定：
其余代码调用 `update()`，结构上就碰不到就绪位。

**watcher 能停下来了。**

```python
w = pool.changes()      # 现在返回 Watch，可迭代、可关闭、也是上下文管理器
w.close()               # 可以从另一个线程或任务关掉
with pool.changes() as w: ...
```

以前 `changes()` 返回生成器，阻塞在事件上而不是停在 `yield` 上，所以别的线程既
`close()` 不了它，设标志位它也看不见。实测：一个非 daemon 线程在里面跑，进程再
也退不出来，`leave()` 也解不开。现在 `leave()` 会关掉所有还活着的 watcher。

**`achanges()` 不再占用 executor 线程。** 改为等在一个心跳会写入的管道上，由事件
循环 select。取消 40 个 watcher 之后再要一个 `asyncio.to_thread`：**3,092ms →
1ms**（24 核机器，默认 executor 28 个 worker 曾经全被卡住）。

**`pool.wait_replacement(slot=/identity=)`** 和异步的 `await_replacement()`：
阻塞到座位由另一个任期接管，返回接任者。`Member.wait_fenced()` 是同一个问题的
自视角，这个是旁观视角。

**每次调用带 `request_id`**，在 `CallContext` 上可读，两侧日志能指着同一次尝试
说话。**不做幂等缓存** —— 被调方无从知道重放是否安全，那个决定属于调用方，
`NotDelivered` / `OutcomeUnknown` 的区分就是为了让它做得出来。

### 修复

**发布得太快会把自己的心跳饿死。** 发布会取消挂起的请求，好让新状态立刻出去。
但顶上来的那个请求又被挂起了 —— 于是下一次发布再把它取消掉，如此往复，一拍都
完不成。实测每秒 379,000 次发布之下，注册表在第 4 秒把这个还在不停说话的成员
**从它自己的池子里删掉了**，而 `beats_ok` 全程停在 0。

修法是：**取消之后顶上来的那个请求不挂起**。它本来就带着新东西，没什么可等的，
一个 RTT 就回来了，状态随即落地。代价是一次往返，不是一整个 hold。

取消路径上另外补了 `MIN_GAP`，把风暴下的请求速率从每秒 24 次压到 11 次。它是
速率上界，**不是**防饿死的那一环 —— 这点实测过，两者互不相干。

> 中途试过一套"发布序号"，用来分辨"取消的是不是一个已经带上这次发布的请求"。
> 实测下来带与不带的数字**一模一样**（5/s 时每次发布 1.00 个请求，50/s 时
> 0.23），所以整套删掉了。留着就是一个没人守得住、也没买到任何东西的分支。

**同值发布不再花代价。** state 和就绪位都和已发布的逐字节相同时，既不敲醒心跳、
也不抬高池子版本。注册表侧本来就不抬版本，客户端侧过去仍会白花一次请求。

**state 和就绪位现在在同一把锁下发布。** `compose()` 过去在 Mutex 里读 state，却
把 ready 当独立原子量读，一次心跳可能带着新 state 和旧就绪位出去。没能复现出来，
但 `ready(**kw)` 的语义就是这一对，所以锁的也该是这一对。

**`ready()` 的合并加了锁。** 合并是读-改-写，两个发布者撞上会丢掉彼此的 key。
今天打不中 —— `MAX_STATE` 把窗口卡得很窄，8 个发布者 300 轮一个没丢，而把窗口
撑开 0.5ms 就丢了 420 个。防的是窗口以后变宽，不是今天的 bug。

### 明确不做

| 请求 | 为什么不做 |
|---|---|
| `hold_ms` 独立暴露 | 它不决定新鲜度。ttl=20s 下发现延迟 41ms，因为变化是**唤醒**挂起的请求，不是等它超时。hold 只管空闲请求率和黑洞连接的察觉速度，暴露出去只会让人为了"更快"把它调小，延迟一点不省而请求量翻几十倍 |
| 服务端状态投影 / predicate | 它真正解决的是"被无关变更吵醒"，不是带宽，而 state 已有 16 KB 上限。需要成员数、state 大小、无关唤醒占比三个真实数字才谈得上，否则做出来大概率是错的抽象 |
| 独立 Queue / streaming 通道 | 同一个 leased-queue 论证已经否过（见 `docs/02-design.md`）。`max_concurrency` 解决的是**无限堆积**，不是隔离：它在 dispatch 之前返回 503，所以拒绝是安全可重试的，但并发槽被长驻 RPC 占满时，control 调用同样吃 503 —— 控制面并没有和数据面分开。目前使用方不跑共享 Actor 的长驻 RPC，所以这条先不做，一旦要跑就得重开 |
| 跨 pool 一致性快照 / 原子 fanout | 每个成员是独立到期的租约，跨 pool"一致"只对注册表账本成立、对现实不成立。原子 fanout 是共识，另一个系统的问题 |
| readiness generation / CAS | 层次错了。线上不存在这个乱序：只有一个心跳循环、顺序发送、注册表存最后值。乱序只在进程内，而那是 `update()` 解决的 |

---

## 0.7.1

**0.7.0 从未上架 PyPI** —— 它的流水线在 `lint and test` 就失败了，wheel 和发布
全部跳过。tag 和 GitHub Release 留着，但那个版本号在 PyPI 上不存在；要 0.7.0 的
内容请装 0.7.1。

### 修复（测试，不是产品）

拦下 0.7.0 的是 `test_a_lossy_link_still_lets_a_member_start`：40% 丢包下跑五次
启动，**零容忍**。而下面那条 slow 测试早就量过——30 秒截止时间下单次启动仍有约
0.8% 会失败，没有归零。五次零容忍等于每跑一次套件约 4% 概率乱叫。

这次它乱叫在了发布上。改成五个种子跑在一条测试里、容忍一次，和那条 slow 测试
同一个做法、同一组数字。把丢包率拉到 95% 它照样变红，判别力还在。

**不是长轮询的回归**，这一点查过：`hold_ms` 初值为 0，只在一次成功 ack 之后才
设置，所以在一拍都没落地的丢包场景下，超时仍是旧的 `interval × 3/4`——join 这
条路径完全没被 0.7.0 碰到。本地 60 次启动零失败也印证了。

---

## 0.7.0

**`ttl_ms` 不再决定变化多久可见。**

原先它一个数管三件事：租约长度、心跳间隔（`ttl/4`）、以及发现延迟的上界（也是
`ttl/4`）。万卡规模下这两个诉求直接打架 —— 调大免得把 GC 停顿判成失联，调小才
能让事件驱动及时。

现在注册中心在没话说的时候**把应答挂起**，池子一动立刻返回。

| ttl | 旧的发现延迟上界 | 实测发现延迟 | 心跳/秒 |
|---|---|---|---|
| 2 s | 500 ms | 21 ms | 2.06 |
| 8 s | 2,000 ms | 19 ms | 0.75 |
| 20 s（默认） | 5,000 ms | **21 ms** | 0.44 |

**默认配置下快了 238 倍，而心跳数量一次都没多。** 自己发布状态仍是即时的
（实测 0.6 毫秒）。

40,000 成员静默态：固定间隔轮询 163 MiB / **622% CPU**，长轮询 325 MiB /
**20.5% CPU** —— 约 4 KB 一条挂起请求，CPU 差 30 倍，因为那些请求原本都在空转。

### 不用改任何配置

**没有新旋钮。** 客户端请求挂起的时长，就是它本来要睡的那个间隔。`ttl_ms` 仍是
唯一的数，只是它现在只管失联判定和流量。

**双向兼容。** `hold_ms` 在 Beat 里而不是只由服务端定：早于这个字段的客户端不发
它，服务端视为 0 立刻应答，行为和以前完全一样；新服务端也不会把旧客户端挂到超时。

### 修复

**`beat_timeout` 的余量是个常数，在短租约下顶过了租约本身。** `hold + hold/4 +
2000ms` 在 ttl=2000 时是 2.6 秒 —— 比租约还长，于是一个丢包就足以掉出名册，而
「一拍必须有界」正是为了防这个。改成按比例，任何租约下都稳定在 0.44 个租约。

---

## 0.6.1

### 修复

**`changes()` 每一拍都产出快照，不管池子有没有动。** 它挂在客户端的拍数计数器
上，而那个计数器每次心跳都会前进 —— 实测 14 秒里产出 25 份快照，其中只有 4 份
是真的变化，84% 是空转。5000 成员时一份快照要 10.6ms，那些全是白重建。更要命的
是消费者还得自己 diff 才知道有没有变，这个 API 的意义基本被抵消掉。

现在铃照旧每拍响（`join`、`flush`、`wait_fenced` 等的是心跳结果），但产出由
**池子自己的 version** 决定。同样的场景：3 份产出，3 次真变化。

**`Snapshot.revision` 给的是客户端拍数，不是池子版本。** 文档承诺的是「每个 pool
有单调 revision」，拿到的却是一个客户端本地的、跨 pool 共享的计数器 —— 两个进程
拿到的数对不齐，`changes(since=)` 也就没法真正表达「从这个位置接着往下看」。
现在它就是池子的 version。

顺带：`snapshot()` 此前分两次读缓存（成员一次、版本一次），中间心跳可以落在当中。
现在和 `epoch()` 一样在一把锁内读完。

---

## 0.6.0

调用方要的三件事：**知道谁在调我、知道调用到底跑没跑、知道自己被换掉了**。
外加把库里最后一处轮询拆掉。

### 新增

**RPC 结果分成四类，而不是三类。**

```python
except tinyray.NotDelivered:    # 确实没送到 —— 方法一定没跑，直接重试
except tinyray.OutcomeUnknown:  # 可能跑了 —— 带同一个 request id 重试，或保证幂等
```

两者都是 `Unreachable` 的子类，既有的 `except Unreachable` 不受影响。

**调用方身份。** 方法签名里标一个 `tinyray.CallContext`，库会填上调用方的
identity / pool / slot / incarnation。调用方那边不用写任何东西。租约因此可以绑
到任期上，而不是绑到一个业务参数上 —— 后者会忘了传，也会传错。

**`Member.wait_fenced()` / `await_fenced()`。** 被更晚的任期顶替时主动醒来。
RPC 层本来就会拒绝打到旧成员的调用，但一个进程手里可能还攥着 GPU、推理服务和
自己的端口，它得被告知才能停掉那些。

**`Member.set_ready(state)` 与 `flush(timeout)`。** 前者整体替换 state；
`ready(**kw)` 是合并语义，所以在此之前**没有任何办法把一个 key 拿回来**。
`flush()` 阻塞到注册中心确实收下，省掉"发布完再反查自己"那一步。

**`Pool.snapshot()` 与 `Pool.changes()`。** 快照包含**入座但还没 ready** 的成员，
每条记录带 `incarnation` 和 `ready`，并带一个单调的 `revision`。`changes()`
阻塞在事件上产出新快照，池子不动就不返回。异步用 `AsyncPool.achanges()`。

给的是**快照流而不是事件流**，这是有意的：客户端以心跳为采样率，注册中心会把一个
间隔内的多次变化折叠成「该成员的当前状态」，所以「不丢事件」是协议兑现不了的承诺，
而「不丢状态」可以。事件是一次 diff 的事。

**`join(max_concurrency=)`。** 给同时执行的调用数封顶，超了返回 `NotDelivered`
而不是排队 —— 拒绝是有界的，排队不是。此前是每连接一个线程且无上限。

**`Member.identity`、`Snapshot.get(identity)`、`Pool.slot(k, require_ready=)`。**

**文档站** —— <https://hspk.github.io/tinyray/>，含上手指南与完整 API 参考。

### 变化

**库内不再有任何轮询。** `_settle`、`wait`、`epoch`、`join` 的首拍等待此前都是
sleep 循环，最紧的一处每秒 500 圈。现在全部阻塞在客户端的条件变量上，心跳落地
才醒。

一条测试直接读源码把这件事钉死 —— 运行时量不出来：实测空转 5 秒，事件驱动
0.0054s CPU、0.05 秒一圈的轮询 0.0109s，只差两倍且被机器噪声盖住，撑不起阈值。

**代码行数预算（2,000 行）不再是硬约束。** 它挡下过一次真的膨胀，但开始挡住
补窟窿的工作。约束换成 review 守，判据不变：*超出预算就是设计错了，不是写多了*。

### 修复

`Pool.changes()` 在成员被顶替后会永久阻塞 —— 心跳循环退出，铃不再响，消费者
等的是一件再也不会发生的事。现在流会有界结束。

---

## 0.5.0

八个静默失败。它们的共同点不是某个模块，而是**同一个事实有两份表示，却没有任何
东西强制它们一致**。

### 变化

**体积预算从「拒发」改成「警告」。** 超过 1 MB 的调用会发出
`tinyray.OversizeWarning` 然后照常送达。原先 `except ValueError` 包住大 payload
的代码将不再捕获到东西 —— 调用本身会成功。

在阈值上硬失败，会把一个从 900 KB 长到 1.1 MB 的返回值变成一次停机；而一次调用
是点对点的，超了只是两端慢。**注册中心的 state 预算仍是硬限**，那条不一样：
state 会复制给每一个订阅者，实测 6 MB 到 20 个订阅者变成 120 MB，它保护的是别人。

**`epoch()` 可能超时**，在过去会返回一轮的场景下。但过去返回的那一轮，其指纹
并不描述自己的成员名单 —— 各 rank 会一致认为「没问题」。

### 修复

**注册中心重启后，客户端会静默持有残缺名单。** 重启清空缓存是对的，但紧接着又
套用了同一个 ack 里的增量 delta，而那个 delta 是按**旧编号**算出来的。实测：两个
成员在册，客户端只持有一个，而且它的 roster 指纹**和服务端完全一致** —— 那正是
epoch 用来判断各 rank 是否一致的东西。

**告别会被「超时但仍送达」的心跳撤销。** 客户端超时只代表放弃等待，请求仍可能
被处理。实测在 200ms 租约下限：300 次 `leave()` 有 6 次成员仍在册，而告别本身
零失败。注册中心现在记住说过再见的任期一个租约周期。

**`epoch()` 冻结的指纹与名单不符。** 指纹按全部占位者算（ready 与否不影响它，
这是有意的），而冻结的名单按 ready 过滤 —— 于是两个 rank 可以拿到**同一个指纹、
不同的名单**，且两边都报告 valid。四路 churn 下实测 2,636,372 / 2,670,992 次
冻结对不上。

**`asyncio.run()` 每次泄漏一个 httpx 客户端和一个文件描述符。** 100 次调用 =
100 个条目、100 个 fd，而默认上限是 1024。缓存键 `id(loop)` 还是不安全的：地址
会被下一个循环复用，实测连续 5 次 `asyncio.run()` 有 2 次撞上已用过的 id。

**分块请求的 body 被静默丢弃。** 没有 content-length，于是长度被算成 0，body
留在 socket 里 —— `echo(x=7)` 当成 `echo()` 执行并返回 200，随后分块框架被当成
下一个请求行解析。现在明确拒绝并关闭连接。

**fork 出的子进程正常退出时永久挂死。** 继承来的 tokio runtime 在解释器收尾时
等一批不存在的线程，子进程卡在原生代码里，父进程的 waitpid 跟着挂。
`DataLoader(num_workers>0)` 就是这个形状。

**异常正文不受出口预算限制。** 一次 `raise ValueError("y" * 8MB)` 在网线上放了
16,777,461 字节，而同样大小的返回值会被拒掉。

**订阅超过 64 个 pool 会直接杀掉成员**（`accepted=false` 的含义是「座位没了，
停止心跳」，于是循环真的停了）。现在在客户端拒绝那次订阅并指出是哪个 pool。

### 测试

`cargo test` 在此之前**一个测试都没有**，现在有 50 个，覆盖注册中心状态机与线上
消息类型。17 个人为植入的回归全部被为它而写的那条测试抓到。

多机与规模已实测：三个容器跨网络命名空间互相发现并调用，指纹一致；10 万成员跑通
且订阅方看到全部，version 恰好 100,000；峰值 216,335 次/秒零错误。

---

## 0.4.1 及更早

见 `git log`。
